<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Forecast</title>
    <link rel="stylesheet" href="css/forecast.css">
</head>
<body>
    <div class="weather-container">
        <h1><%= weatherData.latitude %>, <%= weatherData.longitude %></h1>
        <p>Latitude: <%= weatherData.latitude %> | Longitude: <%= weatherData.longitude %></p>

        <div class="current-weather">
            <h2>Current Weather</h2>
            <p>Temperature: <%= weatherData.hourly.temperature_2m[0] %>°C</p>
            <p>Wind Speed: <%= weatherData.hourly.wind_speed_10m[0] %> km/h</p>
            <p>Wind Direction: <%= weatherData.hourly.wind_direction_10m[0] %>°</p>
            <p>Precipitation: <%= weatherData.hourly.precipitation[0] %> mm</p>
        </div>

        <div class="hourly-forecast">
            <h2>Hourly Forecast</h2>
            <div class="hourly-forecast-grid">
                <% weatherData.hourly.time.slice(0, 8).forEach((time, index) => { %>
                    <div class="forecast-item">
                        <p><%= time %></p>
                        <p>Temp: <%= weatherData.hourly.temperature_2m[index] %>°C</p>
                        <p>Wind: <%= weatherData.hourly.wind_speed_10m[index] %> km/h</p>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>